/* crypto.js removed — logic inlined into crypto.html
   This file intentionally left as a placeholder to avoid build errors
   if other tooling still expects the path. Safe to delete if desired. */

// noop
